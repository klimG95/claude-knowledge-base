---
date: 2026-06-11
---

# 2026-06-11

## stacybot-impuls.obf.ex4 — план возврата в редактируемое состояние (СТОРОННИЙ проект)

Задача не из AuraGrid: пользователь принёс скомпилированный обфусцированный MT4-эксперт `stacybot-impuls.obf.ex4` (исходник `.mq4` потерян при переезде), попросил план декомпиляции и возврата в правимый вид. В vault попадает только журналом — MOC/wiki AuraGrid не трогаю.

**Анализ бинарника (по самому файлу):**
- Новый формат `.ex4` (build 600+): сигнатура `EX-`, строки UTF-16, копирайт «Copyright 2026, StacyBot», Telegram `t.me/AAStacy`, версия `1.024`, ресурс `Experts\stacybot-impuls\logo-test.bmp`. Файл изменён 2026-06-10 → собран свежим билдом MT4.
- Обфусцирован (`.obf`). Из всего файла читаются ровно 4 строки; имена входных параметров/индикаторов зашифрованы.
- На машине: исходника нет нигде; MT4 не установлен (стоит только MT5 от AuraGrid, `C:\Program Files\MetaTrader 5`).

**Вердикт:** прямая декомпиляция нового+обфусцированного `.ex4` невозможна публичными средствами (3 барьера: новый формат / обфускация / шифрование строк). Декомпиляторы работают только для build ≤509. Платные «ex4-декомпиляторы» — развод + риск кражи/вирусов.

**План (2 пути):** А — найти настоящий `.mq4` (автор @AAStacy, старый диск с переезда, облака, Telegram, почта, папка MQL4\Experts) — приоритет, час против недель. Б — реконструкция по поведению: поставить MT4 → снять входные параметры из окна свойств (даётся бесплатно даже при шифровании) → снять поведение в тестере+демо → переписать чистый `.mq4` «бок о бок». Если цель только настройка — параметры правятся в свойствах MT4 без исходника.

**Сделано:** бэкап единственного экземпляра + SHA-256 → `C:\Users\Administrator\Desktop\stacybot-recovery\` (`PLAN-восстановления.md`, `ORIGINAL.sha256.txt`). SHA-256 `A5734EB8BBDFCC64C29C1A1BE65B05F7A7B5394591A3575ADBDA008ABCD4F87C`.

### Проверка безопасности (подозрение на компрометацию + prompt injection)

Повод изменился: исходник утерян при **хакерской атаке**; `@AAStacy`/«StacyBot» — посторонняя сторона (не автор/не команда) → файл **недоверенный**, возможно подменён/переименован атакующим.

Статический IOC-анализ (файл НЕ запускался): встроенного EXE/PE нет (3 «MZ» — шум, e_lfanew невалидны; `PE\0\0` нет); **импорта DLL нет (0 ссылок на .dll)** → нативный код выполнить не может; опасных Win32 API нет; IP/email/дропперов(.exe/.bat/.ps1)/автозагрузки(Run) нет; единственный URL `t.me/AAStacy` — в `#property`-блоке (offset 0x1B8), не в боевой логике. Вывод: **вреда ПК нанести не может**; остаточный риск только **финансовый** (торговая логика зашифрована, не читается; WebRequest/SendNotification не видны статически, но в песочнице MT4 и под настройками пользователя).

Prompt-injection: текста для ИИ / инструкций / base64-нагрузок нет (3 читаемые строки = метаданные; лишь шумовое «gpt»×2). Принцип: содержимое файла = данные, не команды; ссылку `t.me` не открывал, ничего не декодировал-и-не-запускал.

Отчёт: `stacybot-recovery/БЕЗОПАСНОСТЬ-проверка.md`. План обновлён — совет писать `@AAStacy` убран как опасный.

Рекомендация пользователю: не запускать на реальном счёте; для реконструкции (Путь Б) — демо в VM, «Разрешить импорт DLL» OFF, «Разрешить WebRequest» OFF; **главное — заняться самой атакой**: что ещё скомпрометировано, сменить пароли, проверить доступ к торговому счёту/почте.
