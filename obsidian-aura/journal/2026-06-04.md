---
type: journal
tags: [auragrid, incident, impulse, executor, live-trading, journal]
created: 2026-06-04
updated: 2026-06-04
---

# 2026-06-04 — Инцидент: баги Impulse на живом XAU + фикс (защита невиновна)

## Контекст

Пользователь: «После сборки и установки новой версии — множество багов: фантомные сделки, неработающие стоплосы, не закрытие по кнопке. Нужно чтобы софт работал как нужно с защитой». Подразумевалось подозрение, что защита (Фаза 2/6, прошлая сессия) сломала торговлю.

PHASE 1: [[runbook-vault-integration]], [[auragrid-performance-strategy]], [[auragrid-incidents-log]], логи `%APPDATA%\Aura\logs\`.

## Метод — доказательная диагностика по живым логам (не гадание)

Сразу зафиксировал: strip `.py`→`.pyc` (Фаза 2) не может менять торговую логику (pyc ≡ скомпилированный py). Симптомы — торговые, значит код. Дальше — логи и git blame:

1. Свежие логи есть (бот гонялся 31.05–04.06). Magic-диры: `20260001`=Grid, `20260002`=Impulse. **Grid здоров** (`profit_trail_sl_set`, `channel_reset_after_close`, `ingest_ok`) → баги только в Impulse.
2. Частоты Impulse: `impulse_pending_modify_failed` 3261, `trade_retries_exhausted` 3261. **Retcode: 10018 (market closed) ×9783**, 10016 ×2, 10029 ×3. `poll_heartbeat` p99 253-333мс (петля замирала).
3. **git blame решающий:** `impulse.py`/`executor.py`/`validation.py` НЕ менялись после checkpoint `461904b` (только rebrand-нейминг). → защита и perf-рефакторинг **невиновны**; баги латентные в Impulse, проявились на волатильном XAU у DooTechnology. Откат не помог бы — нужны точечные фиксы.

## Фиксы (коммит `62694f3`, PR #36)

- **Bug B (доминанта):** `MARKET_CLOSED` (10018) убран из `executor._RETRYABLE`. Был → ретрай 3×0.2с на закрытом рынке = 0.6с заморозки loop ×9783 (дневной перерыв сессии XAU). Это «окно не отвечает / кнопка не закрывает / хаос». `NO_PRICES` остаётся retryable.
- **Bug A:** initial SL 10016 «Invalid stops» (дистанция от текущей цены в freeze-level). Позиция защищена pre-fill SL (шире), но state писался в проваленное значение → trail от несуществующего уровня. Fix: state = фактический брокерский SL.
- **Bug C:** сервер отвергал telemetry `mode="IMPULSE"` (Literal SCALP/CG + колонка String(5)) → вся Impulse-телеметрия дропалась. Fix: ModeName+=IMPULSE, String(16), миграция 0007.
- **Регрессия моей Фазы 6:** `tests/integration/conftest.py` JWT-ключ <32 валил 11 integration + 12 admin_bot. Пропустил, т.к. после Фазы 6 гонял только server-набор. Урок зафиксирован.

Verify: bot **1328 passed**, server **237 passed**, 0 fail/err. 3 новых теста.

## Грабли / уроки

- Обрезал вывод pytest через `| tail` → потерял список FAILED; пришлось перегонять с `-rfE` в файл. Не обрезать вывод полного прогона.
- **fake-MT5 не воспроизводит брокер** (stops_level/session) → order-execution фиксы требуют demo-валидации. Честно сказал пользователю.

## PHASE 3

- [[auragrid-incidents-log]] — полная запись инцидента 2026-06-04 (Status/Симптом/Диагностика/Root cause/Resolution/Prevention).
- [[CHANGELOG]] — запись сверху.
- auto-memory: новый указатель на инцидент + урок про demo-валидацию.
- Этот journal.

## Часть 2 — perf-регрессии → откат Ярусов 0–2

Пользователь (после первого фикса): «не тот результат что был. Сетка не всегда > минимума, софт сильно медленнее + лаг, закрытие по кнопке ставит стопы по чуть-чуть, трейлинг очень медленный».

Диагноз: сборка содержит perf (poll_heartbeat несёт perf-поля). git blame: код сетки/трейла/скальпинга не менялся — менялся темп `on_tick` (дедуп, Ярус 1) + `scan_all` (Ярус 2b) + async-логи (Ярус 2c). **Дедуп гейтит per-tick операции → медленный трейл / закрытие порциями / лаг; scan_all → грид-спейсинг.** Perf делался ради исходной жалобы на лаги, но дал обратный эффект и не валидировался вживую.

**Откат (`18f18eb`, PR #37):** 5 торговых ярусов (0,1,2a,2b,2c) → `461904b`. Ярус 4 (UI) оставлен. Защита + Impulse-фиксы сохранены. Детерминированно: 6 hot-path файлов из 461904b + переналожен Aura-нейминг; `git diff 461904b` = только нейминг. safety-grep чист. bot **1309 passed**.

Решение откатить, а не чинить хирургически: пользователь воспринимает perf строго хуже (медленнее + баги), цель «как было», 461904b — известно-стабильный (для этого чекпоинт и создавался). Ветка `fix/shelve-perf-restore-stability` = полная продаваемая сборка.

PHASE 3 (часть 2): [[auragrid-incidents-log]] секция «2026-06-04 (часть 2)»; [[auragrid-performance-strategy]] баннер «откачено»; [[CHANGELOG]]; auto-memory; этот раздел.

## Связано с

- [[auragrid-incidents-log]] — запись инцидента (детали + Prevention)
- [[auragrid-impulse-strategy]] · [[adr-004-impulse-adaptive-distance]] — стратегия и источник modify-цикла
- [[auragrid-protection-strategy]] — защита (доказано невиновна)
- [[auragrid-performance-strategy]] — инвариант про живой MT5
